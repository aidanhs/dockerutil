#!/bin/bash
source utils
exception="${1:-.*}"
date
if [ $($DOCKER ps -a | tail -n +2 | wc -l) -gt 0 ]; then
  echo "$DOCKER ps -a | tail -n +2 | grep -v $exception | awk '{print \$1}' | xargs $DOCKER kill"
fi
